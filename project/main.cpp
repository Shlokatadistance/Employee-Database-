
#include <iostream>
int Ispalindrome(int n)
{
    int reverse = 0;
    for (int i = n; i >0; i /= 10  )
    rev = rev*10 + i%10
    return (n == rev);

}
void counter(int first, int last)
{
    for (int i = first , i <= last, i ++)
    if (Ispalindrome(i)){
        for (int x,y,z < last, int x,y,z > first)
        if (i == x*y*z);
        
        cout << i << endl;
        else;
        cout << "Nothing" << endl;
        return 0;
        
        
    }
    else;
    cout << "Wow" << endl;
    return 0;
}
int main(){
    int
}

